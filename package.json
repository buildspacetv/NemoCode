{
  "name": "nemocode",
  "version": "0.15.7",
  "private": true,
  "description": "Use Nebius Token Factory models in Claude Code, OpenCode, Codex, and Pi Code",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "build:bundle": "bash scripts/build-bundle.sh",
    "build:site": "pnpm build:bundle && pnpm --dir site exec convex codegen --typecheck disable && pnpm --filter @nemocode/site build",
    "build:site:preview": "pnpm build:bundle && pnpm --filter @nemocode/site build",
    "bump-version": "bash scripts/bump-version.sh",
    "dev": "pnpm -F @nemocode/cli dev",
    "dev:site": "pnpm --filter @nemocode/site dev",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "hooks:install": "lefthook install",
    "test": "turbo run test --filter @nemocode/tests",
    "test:offline": "turbo run test:offline --filter @nemocode/tests",
    "bench:proxy": "pnpm -F @nemocode/tests bench:proxy",
    "capture:proxy-fixtures": "pnpm -F @nemocode/tests capture:proxy-fixtures",
    "watch": "pnpm dev",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "lefthook": "2.1.9",
    "oxfmt": "0.57.0",
    "turbo": "^2.10.1",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@11.9.0"
}
